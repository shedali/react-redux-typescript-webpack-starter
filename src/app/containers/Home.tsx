import * as React from 'react';

import { Hello } from '../../lib/components/hello-world';

const Home = () => (
    <div>
        <h1>Home :-)</h1>
        <Hello name="Maxim" />
    </div>
);

export default Home;
